DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=rootpass
DB_NAME=agent_foundry

# For SQLAlchemy (aiomysql)
# For SQLAlchemy (aiomysql)
DATABASE_URL=mysql+aiomysql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}
# DATABASE_URL=sqlite+aiosqlite:///./foundry.db
